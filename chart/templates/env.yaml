apiVersion: v1
kind: ConfigMap
metadata:
  annotations: {}
  labels:
    {{- include "mqtt-gateway.labels" . | nindent 4 }}
  name: {{ include "mqtt-gateway.name" . }}-environment
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
