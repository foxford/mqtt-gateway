apiVersion: skaffold/v1alpha5
kind: Config
build:
  artifacts:
  - image: netologygroup/mqtt-gateway
    docker:
      dockerfile: docker/Dockerfile
  tagPolicy:
    envTemplate:
      template: '{{.IMAGE_NAME}}:{{.IMAGE_TAG}}'
  local:
    push: true
    useDockerCLI: true
deploy:
  kubectl:
    manifests:
    - k8s/mqtt-gateway.yaml
    - k8s/mqtt-gateway-headless.yaml
