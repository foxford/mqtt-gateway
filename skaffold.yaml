apiVersion: skaffold/v1alpha4
kind: Config
build:
  artifacts:
  - image: netologygroup/storage
    docker:
      dockerfile: docker/Dockerfile
  tagPolicy:
    envTemplate:
      template: '{{.IMAGE_NAME}}:{{.IMAGE_TAG}}'
  local:
    push: true
    useDockerCLI: true
deploy:
  kubectl:
    manifests:
    - k8s/mqtt-gateway.yaml